[project]
name = "donotfraud-vision"
version = "0.1.0"
description = "Liveness / deepfake / injection signals for onboarding captures."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.32",
  "pydantic>=2.10",
  "python-multipart>=0.0.17",
]
